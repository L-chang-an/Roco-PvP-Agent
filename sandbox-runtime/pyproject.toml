[project]
name = "roco-sandbox-runtime"
version = "0.1.0"
description = "Pinned, isolated runtime for read-only Chat Mode data queries"
requires-python = "==3.12.*"
dependencies = [
  "numpy==2.4.2",
  "pandas==3.0.1",
]

[tool.uv]
package = false

