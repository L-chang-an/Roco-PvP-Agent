"""R 线进化包（agent 层，可 import environment；environment 永不 import 这里）。

R0 交付：analysis（重放分析）/ feedback（μ_f 确定性渲染）/ bench（配对评测）/
league（Elo + α-rank 基架）。后续里程碑按需加模块：memory / reflect / editor /
playbook / pool / meta / credit / smc / health / run。
"""
